# Airframe Outline Source

## Status: PLACEHOLDER — precise trace PENDING

The paths currently in `topView.ts`, `sideL.ts`, and `sideR.ts` are a **stylized
placeholder** Hornet planform, NOT a precise outer mold line. They exist so the app
runs end-to-end. They must be replaced before this is treated as accurate.

## To produce the precise outline (user-owned step)

1. Obtain an F/A-18A/B general-arrangement 3-view that is **public domain or
   open-licensed** (e.g. a US Government / DoD work, or a Wikimedia Commons file with
   an explicit compatible license). Do NOT use a source whose license you cannot confirm.
2. Record it below.
3. Trace the outer mold line of each view to a single SVG `path` `d` string on the
   stated viewBox and replace the exports in the three files.

- File/URL:
- Author:
- License:
- Date retrieved:
- Notes: outline traced to SVG path; outer mold line only.
